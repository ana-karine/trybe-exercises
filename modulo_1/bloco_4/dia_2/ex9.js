let numbers = [];

for (let i = 1; i < 26; i += 1) {
  numbers.push(i);
}

// imprime o resultado da divisão de cada um dos elementos 
// do array numbers por 2
for (let i = 0; i < numbers.length; i += 1) {
    console.log(numbers[i] / 2);
  };